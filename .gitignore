*.iml
.gradle/
/local.properties
/keystore/
/.idea/
/build/
/app/build/
/captures/
.externalNativeBuild
.cxx
.DS_Store

# ---- 签名材料：绝不入库 ----
# 密钥库一旦进 Git，即便后续删除，历史提交里依然可被取出，
# 而 Android 的更新链又强绑定同一把钥匙（换钥匙 = 老用户无法覆盖安装），
# 所以这里必须从第一次提交起就挡住。
*.jks
*.keystore
*.pepk
keystore.properties

# ---- 本地调试/临时产物 ----
# 这些是开发过程中的一次性材料（截图、探针、临时脚本、注释备份），
# 对使用者没有意义，进库只会让仓库变胖、让 diff 变吵。
/_bak_comments/
/_probe/
/_shots/
/_tools/
/.kotlin/

# ---- 协议探查材料：不进公开仓库 ----
# tools/ 下是复刻学校认证链用的抓包产物与复现脚本（含被登录页下发的 JS、验证码图）。
# 它们依赖具体账号/会话才有意义，且含校内系统细节，不适合随代码发布。
/tools/

# ---- 构建输出：Release 包由 GitHub Release 分发，不靠仓库 ----
# releases/ 里是构建产物（单包 11MB+），且可用源码重新构建，
# 提交进 Git 会让每次发版都往历史里塞一份二进制。
/releases/

# ---- IDE / 编辑器 ----
*.swp
*.swo
*~
.vscode/

# ---- 内部文档：不进公开仓库 ----
# 这些是开发/运维说明，面向本机而非使用者，故不随代码发布。
/docs/

# ---- 本机运维脚本：不进公开仓库 ----
# 发版脚本写死了本机的代理端口、证书与 Release 流程，对使用者没有价值；
# 它属于「我这边怎么发布」，不是「这个 App 长什么样」。
/release-to-github.sh

# ---- 助手工作目录 ----
# 会话记忆与工具中间产物，属本机开发资料，不进公开仓库。
/.workbuddy/

# Browser preview runtime logs and screenshots
/preview/.runtime/
/preview/node_modules/
